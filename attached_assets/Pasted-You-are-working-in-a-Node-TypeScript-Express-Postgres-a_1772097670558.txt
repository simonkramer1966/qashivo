You are working in a Node/TypeScript Express + Postgres app running on Replit.

Goal: Clean up and harden the system start process so it is predictable, non-duplicated, and easy to debug.

Current symptoms from logs:
- “New database connection established” appears twice (likely multiple DB init paths / side-effect imports).
- Some services start twice (e.g., Xero health check service started twice; workflow timer processor started twice).
- Provider “registered” logs are duplicated.
- Background jobs start immediately during boot and can emit scary stack traces (e.g. Xero refresh token 400), but should not destabilize startup.

Refactor objectives (must implement):
1) Single startup orchestrator with 3 phases:
   Phase 0 (wiring): load env, validate config, create logger, create ONE db/pool connection, create provider registry (no timers).
   Phase 1 (HTTP): create express app + middleware + routes; start listening; expose /health and /ready endpoints.
   Phase 2 (background): start polling/schedulers/workers only AFTER the server is listening.

2) Add RUN_MODE to control what starts:
   - RUN_MODE=api => only HTTP server
   - RUN_MODE=worker => only background services (no HTTP)
   - RUN_MODE=all (default) => both

3) Make every background service idempotent:
   Each service must implement:
     start(ctx): Promise<void>
     stop(): Promise<void>
   start() must no-op if already running (guard via private running flag).
   No constructors may start timers/intervals/cron jobs.

4) Ensure ONLY ONE DB connection is created:
   Create db/pool once (e.g. in createContext()) and pass it via ctx everywhere.
   Remove any “auto-connect on import” patterns.
   Ensure session store uses same pool if possible (or document if not possible).
   Remove any duplicate “New database connection established” logging.

5) Improve startup logging:
   During boot, log only major milestones.
   At end, print a startup summary with:
   - API listening port
   - Providers enabled/disabled
   - Background services started
   - Any integrations missing env (warn once, not repeatedly)

6) Degrade gracefully on integration errors:
   Xero token refresh 400 should NOT throw unhandled errors during startup.
   If refresh fails with 400, mark tenant disconnected / disable autosync and log a single warning; health check should continue for other tenants.
   Health check should not run immediately on boot (delay by ~60s) or run only on schedule.

Implementation tasks (do them in order):
A) Create new file: server/bootstrap.ts
   Export:
   - createContext(): builds ctx { config, logger, db, providers, ... }
   - createHttpApp(ctx): returns express app
   - startHttpServer(app, ctx): starts listen and resolves with server instance
   - startBackgroundServices(ctx): starts all schedulers/workers based on RUN_MODE
   - stopAll(ctx): stops services on shutdown

B) Rewrite server/index.ts to be minimal:
   - createContext
   - if RUN_MODE includes api => start server
   - if RUN_MODE includes worker => startBackgroundServices AFTER listen, or immediately if worker-only
   - add process handlers:
       process.on('SIGTERM'/'SIGINT') => stopAll then close server
       process.on('unhandledRejection'/'uncaughtException') => log error; attempt graceful shutdown

C) Create a BackgroundServiceRegistry:
   - Holds named services
   - startAll(ctx) with order
   - stopAll()

D) Modify existing services to conform:
   - EmailPolling loop
   - Xero sync schedulers
   - Xero health check
   - PTP breach detector
   - Report scheduler
   - Workflow timer processor
   - Debtor scoring worker
   - Portfolio controller cron jobs
   For each, ensure:
     - no start in constructor
     - idempotent start
     - stop clears timers/intervals/cron
     - logs “started” only once

E) Fix Xero health check startup behavior:
   - Do not run health checks inline in index.ts boot sequence.
   - Schedule it (every 20 minutes) with a delayed initial run (~60s).
   - Wrap per-tenant checks in Promise.allSettled; no single tenant failure kills the run.
   - If refreshAccessToken returns null due to 400 => treat as disconnected; do not throw.

F) Add endpoints:
   - GET /health => basic “ok”
   - GET /ready => checks db connectivity and returns readiness (and optionally service registry status)

Constraints:
- Keep changes minimal and incremental; do not rewrite unrelated business logic.
- Keep existing routes and functionality intact.
- Ensure TypeScript builds and app runs on Replit.
- After refactor, “service started” logs should appear only once each.
- No duplicated DB connection logs.
- App should boot to “API ready” even if Xero token refresh fails.

Deliverables:
1) Provide a short summary of changes made.
2) List files changed and why.
3) Paste the new server/index.ts and server/bootstrap.ts (and any new registry file).
4) Confirm by showing expected startup logs (example) with no duplicates.